# TypeScript type check

Run TypeScript type checking on the project.

## Steps

1. Run `pnpm check` in the erp-pos-web directory
2. If there are errors, list each with file path and line number
3. Read the relevant code and apply fixes
4. Re-run to confirm all errors are resolved
