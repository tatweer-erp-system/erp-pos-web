# Production build

Build the project for production and report any errors.

## Steps

1. Run `pnpm build` in the erp-pos-web directory
2. If the build fails, analyze and fix errors
3. Re-run to confirm success
4. Report the build output size
